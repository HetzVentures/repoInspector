Generic single-database configuration.

Generate new migration from model
```
    alembic revision --autogenerate -m <comment>
```

Apply migration to DB
```
    alembic upgrade head
```

Downgrade migration
```
    alembic downgrade <hash>
```